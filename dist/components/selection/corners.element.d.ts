export class Corners extends Handles {
    render({ width, height, top, left }: {
        width: any;
        height: any;
        top: any;
        left: any;
    }): string;
}
import { Handles } from './handles.element';
